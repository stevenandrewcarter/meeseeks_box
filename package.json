{
  "name": "meeseeks_box_full_stack",
  "version": "1.0.0",
  "scripts": {
    "client": "cd src && cd client && npm start",
    "server": "cd src && cd server && npm start",
    "all": "concurrently --kill-others-on-fail \"npm run-script server\" \"npm run-script client\""
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
